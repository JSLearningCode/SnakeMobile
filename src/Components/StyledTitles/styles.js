import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    container: {
        flex: 1,
    },
    buttonStyle: {
        width: '100%',
        height: '100%',
    },

    text: {
        fontSize: 52,
    },
});
